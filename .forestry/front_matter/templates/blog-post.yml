---
label: Blog post
hide_body: false
fields:
- name: main_image
  type: file
  config:
    maxSize: 64
  label: Main image
  description: Main image
- name: title
  type: text
  config:
    required: false
  label: Title
  description: Title of the page
- name: text_box
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Text box
  description: Box for text
